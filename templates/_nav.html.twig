<nav class="navbar navbar-expand-lg bg-body-tertiary border-bottom">
    <div class="container-fluid">

        {# Logo + nom #}
        <a class="navbar-brand d-flex align-items-center gap-2" href="{{ path('app_main') }}">
            <img src="{{ asset('img/logo.png') }}" alt="Sortir.com" style="width: 40px; height: auto;">
            <span>Sortir.com</span>
        </a>

        {# Burger mobile #}
        <button class="navbar-toggler" type="button"
                data-bs-toggle="collapse"
                data-bs-target="#navbarNav"
                aria-controls="navbarNav"
                aria-expanded="false"
                aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarNav">

            {# GAUCHE : navigation principale #}
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_main') }}">Accueil</a>
                </li>

                {% if app.user %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_profile') }}">Mon profil</a>
                    </li>
                {% endif %}
            </ul>

            {# DROITE : infos utilisateur #}
            <ul class="navbar-nav ms-auto align-items-center gap-3">

                {# Date #}
                <li class="nav-item">
                    <span class="nav-link text-muted small d-none d-lg-block">
                        {{ "now"|date("d/m/Y") }}
                    </span>
                </li>

                {% if app.user %}
                    <li class="nav-item">
                        <span class="nav-link small">
                            Bonjour <strong>{{ app.user.firstname }}</strong>
                        </span>
                    </li>

                    {% if is_granted('ROLE_ADMIN') %}
                        <li class="nav-item">
                            <a class="nav-link fw-semibold"
                               href="{{ path('app_admin') }}">
                                Administration
                            </a>
                        </li>
                    {% endif %}

                    <li class="nav-item">
                        <a class="nav-link text-danger"
                           href="{{ path('app_logout') }}">
                            Déconnexion
                        </a>
                    </li>
                {% else %}
                    <li class="nav-item">
                        <a class="btn btn-primary btn-sm"
                           href="{{ path('app_login') }}">
                            Connexion
                        </a>
                    </li>
                {% endif %}

            </ul>
        </div>
    </div>
</nav>
